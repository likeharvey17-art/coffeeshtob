# The local WordPress used to develop the theme. Rebuilt from scratch by
# .github/scripts/wp-dev.sh, so nothing in it is worth keeping: WordPress core,
# a SQLite database, and a symlink back to the theme in this tree.
.wp-dev/

.DS_Store
dist/
